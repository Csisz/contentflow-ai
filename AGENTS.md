# ShipSeal Delivery Pack - 01-agent-instructions/AGENTS.md

Project: Csisz/contentflow-ai

Generated by ShipSeal.
# AGENTS.md - Csisz/contentflow-ai

> Operating manual for any AI coding agent (Claude Code, Cursor, Codex, Copilot, Devin) working on this repository.

## 1. Project overview
Csisz/contentflow-ai is a Flask project. Languages: Python. Current ShipSeal score: 65/100 (Almost Ready).

## 2. Detected stack
- Flask

## 3. Repository map
- `contentflow_ai/`
- `contentflow_ai/ai/`
- `contentflow_ai/dashboard/`
- `contentflow_ai/ocr/`
- `docs/`
- `tests/`

## 4. Detected package scripts
- No package scripts detected.

## 5. Allowed commands
- No verified test, build, lint, or typecheck command was detected from repository files.

### Manual verification placeholders
- No verified test, build, lint, or typecheck command was detected. Add the repository-specific command to pyproject.toml, Makefile, CI, or development docs before asking agents to rely on it.

## 6. Current readiness status
No critical blocker was detected, but the repository has not reached the AI Coding Ready score threshold.

## Repository-specific AI guidance
The repo is not AI Coding Ready; no critical blocker was detected, but the score still needs improvement.

- Work primarily in contentflow_ai/, contentflow_ai/ai/, contentflow_ai/dashboard/, contentflow_ai/ocr/, docs/, tests/.
- Use no verification commands detected as the verification path when those commands exist.
- Never execute uploaded code as part of ShipSeal scanning; only run repository commands during normal development with human intent.
- Critical blockers from the scan: none.
- MCP readiness is a separate governance layer. Do not use MCP tools to bypass repository rules, secret handling, or review gates.

### Critical blockers
- None.

### Optional improvements
- **Build script or build config present:** Worth +4 points in "Build, test & quality gates".
- **Lint or typecheck script available:** Worth +3 points in "Build, test & quality gates".
- **CI workflow exists:** Worth +3 points in "Build, test & quality gates".
- **AGENTS.md exists:** Worth +6 points in "AI agent instruction readiness".
- **CLAUDE.md / Cursor rules / Codex instructions exist:** Worth +4 points in "AI agent instruction readiness".
- **Documented dev rules / CONTRIBUTING:** Worth +3 points in "AI agent instruction readiness".
- **Commands after changes are documented:** Worth +2 points in "AI agent instruction readiness".
- **.gitignore covers node_modules/dist/build/.next/.env/coverage:** Worth +3 points in "Security & secret handling".
- **CONTRIBUTING or PR workflow docs:** Worth +2 points in "Team workflow & governance".
- **CODEOWNERS or ownership signals:** Worth +2 points in "Team workflow & governance".

## 7. Operating rules
- Make the smallest change that satisfies the task.
- Always read the surrounding code before editing.
- Never invent file paths, env vars, or dependencies.
- Prefer existing utilities over new ones.
- Run the relevant checks below after every change.

## 8. Files and folders to avoid
- `.env`, `.env.local`, `.env.production`, `*.pem`, `*.key`, `credentials.json`, `serviceAccount.json`
- Any generated output: `node_modules/`, `dist/`, `build/`, `.next/`, `coverage/`
- Lock files unless explicitly asked to update dependencies.

## 9. Testing checklist
- [ ] Manually verify the lint command; none was detected by ShipSeal.
- [ ] Manually verify the typecheck command; none was detected by ShipSeal.
- [ ] Manually verify the test command; none was detected by ShipSeal.
- [ ] Manually verify the build command; none was detected by ShipSeal.

## 10. PR / self-review checklist
- [ ] Change is scoped and reversible
- [ ] No secrets, tokens, or credentials added
- [ ] No new TODOs left without an issue link
- [ ] Public APIs documented

## 11. Secret handling
This repo expects secrets via environment variables only. Never hard-code credentials. If you need a new env var, add it to `.env.example` with a placeholder.

## 12. Done criteria
A task is done only when: tests pass, lint/typecheck pass, build succeeds, and the change matches the user's intent without unrelated edits.

## Agent Cost Optimizer
Recommended operating mode: Balanced Context

Expected context use: Balanced context use
Confidence profile: Recommended default

Recommended default for normal development without excessive context or verification cycles.

Reason: Project size (44 files) and detected stack (Flask) suggest that normal development can safely avoid excessive build/test cycles. No project commands were detected, so manual verification guidance should stay explicit.

ShipSeal helps AI coding agents avoid unnecessary context usage and excessive verification cycles by matching the agent workflow to the project and task risk.

### Mode rules
- Read the task, AGENTS.md, and only the files needed for the change.
- Prefer existing project patterns and keep edits scoped.
- Avoid unnecessary full repository scans after tiny edits.
- Use full test/build only when the touched surface justifies it.

### Verification expectations
- Run targeted tests when practical.
- Provide manual full verification commands if full test/build was not run.
- Summarize changed files and behavior clearly.

### Expected tradeoffs
- Uses enough context for safe edits without reading everything.
- Prefers targeted checks for small changes.
- Escalates to full verification when shared or risky code changes.

### Scan signals used
- Stack: Flask
- Languages: Python
- Frameworks: Flask
- Build commands: not detected
- Test commands: not detected
- Key folders: contentflow_ai, contentflow_ai/ai, contentflow_ai/dashboard, contentflow_ai/ocr, docs, tests
- Generated/vendor folders ignored: none reported
- Existing instruction files: README.md